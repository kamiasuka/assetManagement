<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.tedu.asset.manage.dao.persist.mapper.AssetMapper">

    <select id="listAllCategory" resultType="java.lang.String">
        select distinct type from asset_info;
    </select>
    <select id="listAllType" resultType="cn.tedu.asset.manage.pojo.vo.TypeListVO">
        select distinct type from asset_info;
    </select>
    <select id="listAssetByCategory" resultType="cn.tedu.asset.manage.pojo.vo.AssetVO">
        select code,name,type,dept,unit,life,amount,use_status,review_status,approval_date
        from asset_info where type = #{type} and review_status = '已通过'
    </select>
    <select id="listByLevel" resultType="java.lang.String">
        select name from asset_category where level = #{level};
    </select>
    <select id="numberSumQuery" resultType="java.lang.Integer">
        select COUNT(*) from asset_info WHERE max_type = #{maxType};
    </select>
    <select id="worthSumQuery" resultType="java.lang.Double">
        select SUM(amount) from asset_info WHERE max_type = #{maxType};
    </select>

<!--   报表导出 -->
    <select id="export" resultType="cn.tedu.asset.manage.pojo.po.AssetPO">
        select code,name,type,dept,unit,life,amount,use_status,review_status,approval_date
        from asset_info where review_status = '已通过'
    </select>

    <select id="exportVo" resultType="cn.tedu.asset.manage.pojo.vo.AssetVO">
        select code,name,type,dept,unit,life,amount,use_status,review_status,approval_date
        from asset_info where review_status = '已通过'
    </select>

    <select id="exportNoReview" resultType="cn.tedu.asset.manage.pojo.po.AssetPO">
        select code,name,type,dept,unit,life,amount,use_status,review_status,approval_date
        from asset_info where review_status = '审核中'
    </select>

    <!--资产录入-->
    <insert id="insertNew">
        insert into asset_add_info(code, name, type, max_type,dept,unit,life,amount,use_status,review_status,submit_date,approval_date)
        values (#{code},#{name},#{type},#{maxType},#{dept},#{unit},#{life},#{amount},#{useStatus},#{reviewStatus},#{submitDate},#{approvalDate})
    </insert>
    <select id="getAddNew" resultType="cn.tedu.asset.manage.pojo.vo.AssetAddVO">
        select code,name,type,max_type,dept,unit,life,amount,use_status,review_status,approval_date
        from asset_add_info where  code = #{code}
    </select>

    <insert id="saveAddNew">
        insert into asset_info(code, name, type, max_type,dept,unit,life,amount,use_status,review_status,approval_date)
        values (#{code},#{name},#{type},#{maxType},#{dept},#{unit},#{life},#{amount},#{useStatus},#{reviewStatus},#{approvalDate})
    </insert>

    <update id="updateAddInfo">
        update asset_add_info
        set name = #{name},type = #{type},max_type = #{maxType},dept=#{dept},unit=#{unit},life=#{life},amount=#{amount},
            use_status=#{useStatus},review_status=#{reviewStatus},approval_date=#{approvalDate}
        where code = #{code}
    </update>

    <update id="updateAddOff">
        update asset_add_info
        set review_status='未通过'
        where code = #{code}
    </update>

    <delete id="deleteAddNew">
        delete from asset_info
        where code=#{code};
    </delete>


    <!--资产变更-->
    <select id="listAllAdd" resultType="cn.tedu.asset.manage.pojo.vo.AssetAddVO">
        select code,name,type,dept,unit,life,amount,use_status,review_status,submit_date,approval_date
        from asset_add_info
    </select>

    <select id="listAllChange" resultType="cn.tedu.asset.manage.pojo.vo.AssetAddVO">
        select code,name,type,dept,unit,life,amount,use_status,review_status,submit_date,approval_date
        from asset_change_info
    </select>


    <update id="assetUpdate">
        update asset_info
        set review_status='审核中'
        where code = #{code}
    </update>

    <delete id="idempotent">
        delete from asset_change_info
        where code = #{code}
    </delete>

    <delete id="addDelete">
        delete from asset_add_info
        where code = #{code}
    </delete>

    <insert id="ReviewAssetUpdate">
        insert into asset_change_info(code, name, type, max_type,dept,unit,life,amount,use_status,review_status,submit_date,approval_date)
        values (#{code},#{name},#{type},#{maxType},#{dept},#{unit},#{life},#{amount},#{useStatus},#{reviewStatus},#{submitDate},#{approvalDate})
    </insert>

    <select id="getSubmitAsset" resultType="cn.tedu.asset.manage.pojo.dto.AssetUpdateDTO">
        select code,name,type,max_type,dept,unit,life,amount,use_status,review_status,submit_date,approval_date
        from asset_change_info where  code = #{code} and review_status = '审核中'
    </select>

    <update id="saveSubmitAsset">
        update asset_info
        set name = #{name},type = #{type},max_type = #{maxType},dept=#{dept},unit=#{unit},life=#{life},amount=#{amount},
            use_status=#{useStatus},review_status=#{reviewStatus},approval_date=#{approvalDate}
        where code = #{code}
    </update>
    <update id="updateChangeInfo">
        update asset_change_info
        set review_status='已通过',approval_date=NOW()
        where code = #{code}
    </update>
    <update id="updateChangeOff">
        update asset_change_info
        set review_status='未通过',approval_date=NOW()
        where code = #{code}
    </update>

    <select id="pageTest" resultType="cn.tedu.asset.manage.pojo.vo.AssetVO">
        select *
        from asset_info group by id
    </select>

</mapper>